{
  "name": "jurix-frontend-tests",
  "private": true,
  "description": "Security tests for the chat frontend (XSS surface). Dev-only: nothing here ships to production.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "puppeteer-core": "^24.0.0"
  },
  "engines": {
    "node": ">=20.11"
  }
}
