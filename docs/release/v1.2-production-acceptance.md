# v1.2 production acceptance

A promoção exige todos os itens abaixo:

- facade de ingestão aplicada e verificada;
- índice HNSW/IVFFlat com `vector_cosine_ops` presente;
- consulta vetorial real usando índice no `EXPLAIN` em modo strict;
- benchmark jurídico live com pelo menos 90% dos casos aceitos e 95% de correspondência de fonte;
- release contract e documentation contract aprovados;
- staging disponível para o benchmark live;
- working tree limpo e tag apontando para o commit exato da release.
